sum = 0
fib_a = 0
fib_b = 1

while sum < 4000000 {
    # Calc the current value
    fib = fib_a + fib_b

    # If it's even, add.
    if fib mod 2 == 0, sum = sum + fib

    # Swap
    fib_a = fib_b
    fib_b = fib
}

print(sum, "\n")
